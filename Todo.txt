
TODO

pass "param bindings" to binding builders, so canActivate (& other) param invocations will be taken care of.



For later (Un-important)
Convert all Template(..) to .. {abc} ...".format(abs = ...)
merge separate1 & separate. Better comments (put eg.s in str)
